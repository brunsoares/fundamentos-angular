import { DepartQuantityValidatorDirective } from './depart-quantity-validator.directive';

describe('DepartQuantityValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DepartQuantityValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
