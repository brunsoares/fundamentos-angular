import { InvalidTextValidatorDirective } from './invalid-text-validator.directive';

describe('InvalidTextValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidTextValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
